<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Controle de Caixa - Login</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  <style>
    body {
      background: #fff;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }
    .logo {
      display: block;
      margin: 0 auto;
      width: 268px;
      height: auto;
    }
    .title-caixa {
      color: fuchsia;
      text-align: center;
      font-size: 30px;
      margin-top: 20px;
      font-weight: bold;
    }
    .container-login {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 30px;
      width: 100%;
      max-width: 400px;
    }
    .field {
      display: flex;
      align-items: center;
      justify-content: flex-start;
      margin-bottom: 20px;
      width: 100%;
    }
    .label {
      color: fuchsia;
      font-weight: bold;
      width: 120px;
      text-align: right;
      margin-right: 10px;
    }
    .input {
      border: 2px solid fuchsia;
      width: 200px;
      border-radius: 8px;
      padding: 8px;
      font-size: 16px;
    }
    .button-entrar {
      background: linear-gradient(90deg, #ff69b4, #c3a1e5);
      color: #fff;
      font-weight: bold;
      border: none;
      border-radius: 8px;
      padding: 10px 30px;
      font-size: 18px;
      margin-top: 10px;
      cursor: pointer;
      transition: box-shadow 0.2s;
    }
    .button-entrar:hover {
      box-shadow: 0 2px 8px rgba(255, 105, 180, 0.2);
    }
    .mensagem {
      text-align: center;
      margin-top: 10px;
    }
    .cadastro-link {
      text-align: center;
      margin-top: 20px;
      font-size: 16px;
    }
    .cadastro-link span {
      color: fuchsia;
      font-weight: bold;
    }
    .cadastro-link a {
      color: #ff1493;
      font-weight: bold;
      text-decoration: none;
      margin-left: 6px;
    }
    @media (max-width: 500px) {
      .container-login { max-width: 95vw; }
      .label { width: 90px; font-size: 15px; }
      .input { width: 120px; font-size: 15px; }
    }
  </style>
</head>
<body>
  <img src="/assets/logo_bomboniere.png" alt="Logo Bomboniere" class="logo">
  <div class="title-caixa">Controle de Caixa</div>
  <form class="container-login" method="POST" action="/login">
    <div class="field">
      <label class="label">Login</label>
      <input class="input" type="text" name="login" required>
    </div>
    <div class="field">
      <label class="label">Senha</label>
      <input class="input" type="password" name="senha" required>
    </div>
    <button class="button button-entrar" type="submit">Entrar</button>
    <% if (message) { %>
      <div class="mensagem has-text-danger"><%= message %></div>
    <% } %>
    <div class="cadastro-link">
      <span>Não tem uma conta?</span>
      <a href="/register">Cadastrar-se</a>
    </div>
  </form>
</body>
</html>
